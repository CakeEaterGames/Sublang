!include "libs/includes.txt"
 
function main()
{
	!var to 3
	!var cur 0
	!var n3 -3
	!var a 0

	jump(skipFuncs)
		writeInt: _writeInt(),
		writeStr: _writeStr(),
		mul: _mul(),
	skipFuncs:
 
	set(cur,s1)
	writelnStr(cur)
	writelnStr(cur)
	set(cur,s2)
	writelnStr(cur)
	set(cur,s3)
	writelnStr(cur)

	jump(-1)
	
	!vars_location
	
	s1: s1p,
	s2: s2p,
	s3: s3p,

	s1p: !stringz "Hello",
	s2p: !stringz "World",
	s3p: !stringz "Planet",

	_stack: 0,
	!zeroes 10
	
}
